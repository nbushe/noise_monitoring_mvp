#!/bin/bash

echo "🔄 Обновляем список пакетов системы..."
sudo apt update

echo "🔍 Устанавливаем необходимые зависимости для Docker..."
sudo apt-get update
sudo apt-get install -y ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings

echo "🔐 Загружаем официальный GPG-ключ Docker..."
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc
echo "✅ GPG-ключ Docker успешно добавлен и защищён от изменений."

echo "📦 Добавляем репозиторий Docker в источники APT..."
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
echo "✅ Репозиторий Docker добавлен."

echo "🔄 Обновляем список пакетов с новым репозиторием..."
sudo apt-get update

echo "🐳 Устанавливаем Docker и компоненты: Docker CE, CLI, containerd, buildx, compose..."
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
echo "✅ Docker и необходимые плагины успешно установлены."

echo "🐍 Устанавливаем Python и pip для дальнейшей работы..."
sudo apt install -y python3 python3-pip python3.12-venv
echo "✅ Python и инструменты разработки установлены."

# Создание виртуального окружения
VENV_DIR="venv"
echo "📁 Создаём виртуальное окружение в папке '$VENV_DIR'..."
python3 -m venv "$VENV_DIR"
echo "✅ Виртуальное окружение '$VENV_DIR' успешно создано."

# Активация виртуального окружения
echo "⚡ Активируем виртуальное окружение..."
source "$VENV_DIR/bin/activate"
echo "🟢 Виртуальное окружение активировано. Текущий интерпретатор: $(which python)"

echo "📦 Устанавливаем зависимости Python из backend/requirements.txt..."
pip install -r backend/requirements.txt
echo "✅ Зависимости Python установлены."

# Проверка прав суперпользователя
if [ "$EUID" -ne 0 ]; then
  echo "⚠️  Этот скрипт требует прав суперпользователя для настройки Docker."
  echo "🔁 Перезапускаем скрипт с sudo..."
  exec sudo bash "$0" "$@"
  exit
fi

# Настройка DNS для Docker
DNS_CONFIG='{"dns": ["8.8.8.8", "8.8.4.4"]}'
echo "🌐 Настраиваем DNS-серверы Docker: 8.8.8.8 и 8.8.4.4..."
echo "$DNS_CONFIG" > /etc/docker/daemon.json
echo "✅ Файл /etc/docker/daemon.json создан с DNS-настройками."

echo "🔄 Перезапускаем службу Docker для применения настроек..."
systemctl restart docker
echo "✅ Docker перезапущен."

echo "🔄 Включаем автозагрузку Docker при старте системы..."
systemctl enable docker 2>/dev/null || echo "ℹ️  Docker уже включён в автозагрузку."

echo "📋 Копируем шаблон .env файлов... (вы можете позже его настроить вручную, если надо)"
cp ./.env.example .env
cd backend/
cp ./.env.example .env
cd ..
echo "✅ Файлы .env успешно созданы из шаблонов."

echo "🎉 Настройка завершена! Теперь вы можете запускать приложение с помощью Docker."
echo "💡 Например: docker-compose up --build"

echo "🔐 Выполняем вход в Docker (введите ваши учётные данные при необходимости)..."
docker login